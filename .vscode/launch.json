{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Api",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/api",
            "cwd": "${workspaceFolder}"
        }
    ]
}